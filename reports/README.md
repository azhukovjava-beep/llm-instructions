# Sync Reports

This directory stores deterministic artifacts produced by the sync pipeline.

Current files:

- `latest-sync-report.md` - human-readable summary of what the sync pipeline renders.
- `generated-files.json` - machine-readable list of generated files and their roles.

These files are regenerated by `scripts/sync-platforms.ps1`.
